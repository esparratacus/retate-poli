<%= form_with(model: @challenge, local: true) do |form| %>
    <% if @challenge.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

        <ul>
          <% @challenge.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class="row">
      <div class="col s12">
        <div class="row">
          <div class="input-field col s10"><i class="material-icons prefix">title</i>
          <%= form.text_field :name ,placeholder:'el nombre de tu reto'%>
          <%= form.label :name,'Nombre'%>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s10"><i class="material-icons prefix">descriptions</i>
          <%= form.text_field :description,placeholder:'describe tu reto'%>
          <%= form.label :description,'Descripción'%>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s10"><i class="material-icons prefix">descriptions</i>
          <%= form.text_area :objective,placeholder:'el objetivo de tu reto'%>
          <%= form.label :objective,'Objetivo'%>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s10"><i class="material-icons prefix">images</i>
          <%= form.file_field :challenge_image,placeholder:'el objetivo de tu reto'%>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s3">
            <%= form.button class: "waves-effect waves-light btn ",type:'submit' do |btn|%>
              Guardar
            <%end  %>
          </div>

        </div>
      </div>
    </div>


<% end %>
